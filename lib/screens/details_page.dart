import 'package:flutter/material.dart';

class DetailsPage extends StatelessWidget {
  static const routeId = '/details-page';
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
